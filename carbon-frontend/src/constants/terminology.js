// src/constants/terminology.js
// Canonical user-facing labels. A "Module" (code) is surfaced as a "Data Product".
// Change here to switch the label platform-wide (e.g. to "Dataset").
export const DATA_PRODUCT = 'Data Product';
export const DATA_PRODUCTS = 'Data Products';

// GHG scope labels/options shared by list + detail + forms.
export const SCOPE_LABEL = { 1: 'Scope 1', 2: 'Scope 2', 3: 'Scope 3' };
export const SCOPE_OPTIONS = [1, 2, 3];
