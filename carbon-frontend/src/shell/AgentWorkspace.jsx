// src/shell/AgentWorkspace.jsx
// Chat-first Agent remake — public name for the Agent body. Implementation
// still lives in AITaskPanel during the migrate (handlers + stream state);
// this module is the stable import for AIWorkspace and new tests.
export { default } from './AITaskPanel';
export { default as AITaskPanel } from './AITaskPanel';
