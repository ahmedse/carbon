// src/components/Wizard/index.js
// Barrel for the reusable Wizard primitive — consumers import from
// '../components/Wizard' instead of reaching into the implementation file
// (mirrors the ai/ barrel pattern).
export { default } from './Wizard';
