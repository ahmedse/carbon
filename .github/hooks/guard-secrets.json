{
  "description": "PreToolUse secret guard — blocks hardcoded API keys / passwords / tokens on edit/write tools. Host must enable Copilot (or compatible) hooks for this file to run automatically.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "edit|write|create|ApplyPatch|replace|insert",
        "hooks": [
          {
            "type": "command",
            "command": ".ai-toolkit/scripts/guard.sh"
          }
        ]
      }
    ]
  }
}
