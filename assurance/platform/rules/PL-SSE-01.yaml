id: PL-SSE-01
journey: Live stream
meaning: The product board uses its own event stream. Pulse chat SSE stays on the AI routes.
source: backend/core/assurance_api.py
owner: platform
failure: Assurance status is narrated by Chat or scraped from markdown.
validation: GET /assurance/stream/ returns text/event-stream for a staff user. It re-reads a ledger file. It does not run pytest.
implementation: AssuranceStreamView. Route is /admin/assurance, not My, Team, or People.
confidence: high-code
catalogue: configured
residual: The stream does not launch tests. A row stays at its catalogue label until a ledger event exists for that commit.
