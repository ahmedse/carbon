# ADR-0041 Ops Canvas — implementation evidence (2026-09-18)

**Status:** Phases 1–5 + FlightDirector QoS + GradeVance/Agent affordances — **COMPLETE**

## Delivered

| Phase | Deliverable | Location |
|-------|-------------|----------|
| 1 | Host shell + canvas shelf | `OpsCanvasShelf` replaces Artifacts panel in `AIWorkspace` |
| 2 | Agent Job Map emit | `PlansService.create_plan` → upsert `job_map` artifact |
| 3 | Chat Job Brief | `CarbonIntelligence._maybe_emit_chat_job_brief` (multi-hop / ask) |
| 4 | Record attach | `OpsCanvasAttachButton` on Employee 360 + GradeVance Run workbench |
| 5 | Skill + share | `engine/skills/ops_canvas_job_map/SKILL.md` · `POST …/artifacts/{id}/share/` |
| + | FlightDirector → live_run.qos | `patch_live_run_qos` from `FlightDirector.build_acceptance_report` |
| + | Agent run affordance | Job Map button on `AgentRunSurface` (conversation-scoped shelf) |

## Schema

`AIArtifact.artifact_type = "job_map"` · `content_json.kind = "job_map"` · five layers
(intent / job_map / live_run / evidence / outcome). Closed kit only.

## API

- `POST /carbon-api/ai/workspace/artifacts/job-maps/`
- `GET  /carbon-api/ai/workspace/artifacts/?artifact_type=job_map&related_type=&related_id=`
- `POST /carbon-api/ai/workspace/artifacts/{id}/share/`
- `GET  /carbon-api/ai/workspace/artifacts/shared/{token}/`

## Tests

- `ai/tests/test_ops_canvas.py` — 5 pure-unit PASS; 2 django_db (upsert/share + QoS patch) need live Postgres
- `OpsCanvasHost.test.jsx` — layers + QoS chips PASS
- `AgentRunSurface.test.jsx` — Job Map control PASS

## Attach surfaces

- People: `EmployeeDetailPage` → `relatedType=people.Employee`
- EduOS: `RunWorkbenchPage` → `relatedType=gradevance.AssessmentRun`
- Agent: `AITaskPanel` → `AgentRunSurface` Job Map dialog
