# PV2-6B confirm-path verify — 2026-09-23 (not a soak night)

`--live --i-have-stack-hold` **without `--record`**. Night 2026-09-23 FAIL stays in the ledger. Streak **0/5**.

Human said next. STACK-HOLD 20260923-19. Elapsed 25 s. Exit 1.

| Journey | Chat | Slots | Approve | Confirm | Plan | Host list | host_row |
|---|---|---|---|---|---|---|---|
| leave | handoff_agent | yes | 200 | **400 ×3** | paused | 200 | no |
| loan | handoff_agent | yes | 200 | **200** | completed | 200 | **yes** |
| attendance | handoff_agent | yes | 200 | **200** | completed | 200 | **yes** |

RULE_21 `/steps/confirm/` is the missing write. Loan and attendance now persist ESS rows. Leave confirm 400: catalog body requires `end_date`; the smoke sent only `leave_type` + `start_date` + `days`. `confirm_body_for` now derives `end_date` for a 1-day leave. Not re-run (would create a second loan/attendance row). Next official night uses the end_date body.
