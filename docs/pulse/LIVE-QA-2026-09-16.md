# Pulse Live QA — Session 2026-09-16

**Roles:** Master + QA · **Host:** localhost:5179 / :8009 · **Brand:** nibras  
**Accounts used:** `ahmed` / `AdminPa_132` (also verified `admin` / `AdmNibras_132` token OK)

---

## Verdict

Your instinct is right: **Agent UX/workflows are not consistent enough for a “coworker” product.** Chat can answer grounded facts sometimes, but presentation and Agent planning lifecycle are unreliable live.

| Area | Live grade |
|------|------------|
| Chat grounding (headcount) | Pass (530 matches DB `is_active`) |
| Chat UX consistency | Fail |
| Agent plan lifecycle | **Fail (P0)** |
| Mode contract header | Pass (text switches correctly) |
| Task list hygiene | Fail (duplicates / failed clutter) |
| Platform apps after restart | Flaky (“No Applications Available” until refetch) |

---

## What we opened for you to see

- Scenario bank: `docs/pulse/QA-CHAT-AGENTIC-SCENARIO-BANK.md`
- Scoreboard canvas: `pulse-qa-measurement.canvas.tsx`
- Control-plane canvas: `pulse-enterprise-control-plane.canvas.tsx`

---

## Live defects (observed)

### P0 — Agent stuck: discovering + 0 steps

1. Switched to **Agent** — header correctly: *“Describe an outcome… plan before doing”* → after brief: *“Review the plan. Nothing runs until you approve.”*
2. Sent: *list leave balances for 3 employees in org unit 8 — plan only*.
3. Task appeared in **MY TASKS** as **`discovering`**.
4. Detail pane: **Needs review** + **0 STEPS** + *“No steps were planned.”*
5. **Run plan** stayed disabled. Status still `discovering` after ~2+ minutes.

→ Contract text promises a reviewable plan; UI shows empty plan. **Workflow broken for new Agent briefs.**

### P0 — Chat chart vs claim mismatch

- Q: active employee count → answer **“We have 530 active employees.”** (DB confirms 530 `is_active=True`).
- Same bubble: chart titled **Active Employee Count** with **“No data”**.

→ Looks untrustworthy even when the number is right.

### P1 — Language / format ignore (prior thread)

- User: Arabic payroll report *“with figures and charts”*.
- Pulse answered in **English**, org units as raw IDs **8, 9, 23**, no usable charts.

### P1 — Agent task list pollution

- **15 tasks**; many near-duplicate *“Analyze headcount…”* entries; mix of Completed / Failed / Cancelled.
- Hard to find the active coworker run; delete-only chrome dominates the list.

### P1 — Presence says “Pulse offline”

- Composer footer showed **Pulse offline** while Chat still answered (presence probes `:9100`; AI goes through Django `:8009`). Misleading status.

### P2 — Home apps race

- After `./manage.sh start`, home briefly **No Applications Available** while Pulse still open; after wait/refetch, People/My/Team returned. Fail-closed apps + restart timing.

### P2 — People card role chips

- “People Admin / Data Owner / Analyst” still look **scrambled/overlapping** on home cards (separate from Policies grid fix).

---

## What worked

- Login `ahmed` / `AdminPa_132`
- Chat ↔ Agent toggle + ADR-0014 contract strings update
- Chat new thread (2nd New chat click) + send + stream
- Headcount **530** grounded to People
- Agent creates a task row from composer brief
- Historical Completed/Failed tasks visible

---

## Credentials noted (for QA matrix)

| User | Password | Role |
|------|----------|------|
| ahmed | AdminPa_132 | Admin |
| admin | AdmNibras_132 | Nibras admin |
| any employee | mozafNibrasPa_132 | ESS (assumed; employee username TBD per seed) |

---

## Next fix order (Master)

1. **Agent planner:** never leave `discovering` + empty steps without terminal Failed + reason; emit real plan DAG or honest refuse.
2. **Chart integrity:** no chart chrome when series empty; or bind series to the same oracle as prose.
3. **Presence:** “Pulse offline” only if chat/agent path is actually down — not `:9100` alone.
4. **Task inbox:** collapse duplicates; default filter Active; status transitions consistent with header.
5. Codify these as L3 Playwright fixtures from `PC-*` / `PA-*` bank (S0–S1).

---

## Chat Deep Journey Wave A (Master refresh)

**When:** 2026-09-16 · **Seat:** QA Validator (Pulse) · **API:** `ahmed` · **Detail:** `docs/pulse/evidence/CHAT-WAVE-A-2026-09-16.md`

| Step | Result | Observed |
|------|--------|----------|
| A0 | PASS | Chat advisory header; `conversation_type=chat` |
| A1 | PASS | Capabilities; no silent writes |
| A2 | PASS | Refuse delete / advisory |
| A3 | PASS | AR عبرار انصاري → **1021** |
| A4 | PASS | EN Abrar… → **1021** |
| A5 | PASS | Lookup **1021** (salary leaked in prose — note) |
| A6 | PASS | Mohammad → multi-list, no bluff |
| A7 | PASS | **530** = DB `is_active` |
| A8 | PASS | pass^3 **530/530/530** |
| A9 | **FAIL** | Kuwaiti aggregate `FieldError: nationality_code`; no number; EN canned error |
| A10 | PARTIAL | ahmed API prose 530 no empty-chart meta; emp_1001 UI 0 + empty chart + Pulse offline |

**Blockers for Wave A DoD:** A9 P0 (alias + KWT vs KW); A10/M17 trust + M12 language still open.
