# Generated by hand for Phase P1C — repoint TurnKeyModelLink.dataset_version
# from datahub.DatasetVersion to catalog.DatasetVersion (adopted in catalog
# 0007_adopt_datasets). Runs after the catalog table renames, so the DB FK
# constraint is recreated against catalog_datasetversion.

import django.db.models.deletion
from django.db import migrations, models


class Migration(migrations.Migration):

    dependencies = [
        ('turnkey', '0001_initial'),
        ('catalog', '0007_adopt_datasets'),
    ]

    operations = [
        migrations.AlterField(
            model_name='turnkeymodellink',
            name='dataset_version',
            field=models.ForeignKey(on_delete=django.db.models.deletion.PROTECT, related_name='turnkey_links', to='catalog.datasetversion'),
        ),
    ]
