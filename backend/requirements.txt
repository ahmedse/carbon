# Core Django Dependencies
Django==5.2.3                          # Main framework
djangorestframework==3.16.0           # REST API framework
django-cors-headers==4.7.0            # CORS support
django-filter==25.1                   # Filtering utilities
django-debug-toolbar==5.2.0           # Development debugging
django-silk==5.3.2                    # Development request profiling (P12)
djangorestframework-simplejwt==5.5.0  # JWT Authentication
drf-spectacular==0.30.0                 # OpenAPI schema generation (ADR 0003)

# ASGI/WSGI Server
gunicorn==26.0.0                      # WSGI HTTP server

# Database
psycopg2-binary==2.9.10               # PostgreSQL adapter
sqlparse==0.5.3                       # SQL formatting

# Data & Computation
pandas==2.3.0                         # Data manipulation

# Environment and Utilities
python-dotenv==1.1.0                  # Env variable management

# Logging
python-json-logger==2.0.7             # JSON structured logging

# Testing
pytest==9.1.1
pytest-django==4.12.0
pytest-asyncio==1.4.0
pytest-cov==7.1.0

# HTTP Client
requests==2.34.2                      # HTTP requests (pulse_auth, external APIs)

# Email
django-anymail==12.0                  # Multi-provider email backend (Brevo/SendGrid/Mailgun)

# Cache
redis==5.0.1                          # Memory cache / message broker

# Reporting
xlsxwriter==3.2.2                     # Excel export for GHG reports (E3-1)
python-docx==1.1.2                    # Word (.docx) generation for AI study exports
openpyxl==3.1.5                       # Excel (.xlsx) generation for AI study exports

# ── Vendored Pulse Engine (backend/ai/engine) — INERT, import-only (ADR-0009) ──
sqlalchemy[asyncio]>=2.0.36,<2.1    # Engine ORM (Phase 1 inert; Phase 2 swaps to Django Store)
pydantic>=2.7.0,<3                  # Schema models (core/config.py, models)
pydantic-settings==2.6.0            # Engine Settings (core/config.py)
openai==2.30.0                      # LLM provider client (llm/provider.py)
tenacity>=8.0                       # Retry/resilience (llm/router.py, agent)
cryptography==44.0.0                # Host token encryption (core/encryption.py)
httpx>=0.27.0,<1.0                  # Async HTTP client (agent/api_discipline.py)
PyYAML>=6.0.2                       # Archetype/instance config parsing (ingestion, core)
Jinja2==3.1.6                       # Archetype template rendering (core/archetypes.py)
apscheduler==3.10.4                 # Proactive/cognition background loops
mcp>=1.0                            # MCP tool client (agent/mcp_client.py)
RestrictedPython>=8.0               # Code-snippet skill sandbox (skills/sandbox.py)
PyJWT>=2.9.0                        # JWT auth (agent/tools.py)
